import { test, expect } from '@playwright/test';

/**
 * Verify search results display only relevant products
 *
 * Test Case ID: 1235
 * Priority: P0
 * Coverage: n/a
 * Steps:
   *   1. Enter a valid product keyword.
   *   2. Click the Search button.
   *   3. Review the displayed products.
 * Expected Result: Only products matching the search keyword are displayed.
 * Test Data: product_search_data.searchKeyword
 *
 * Generated by LevelUp AI QA Engine (deterministic test-case build)
 * Base URL: https://automationexercise.com/
 */

test.describe('Verify search results display only relevant products', () => {
  test('Verify search results display only relevant products', async ({ page }) => {
  // @tc:TC1235
    await page.goto('https://automationexercise.com/');
    await page.waitForLoadState('domcontentloaded');
    
    // Enter a valid product keyword.
    // NOTE: step not auto-mapped — review manually.
    
    // Click the Search button.
    await page.getByRole('button', { name: 'Login' }).click();
    
    // Review the displayed products.
    await page.locator('#search_product').first().click();
    await page.waitForLoadState('domcontentloaded');

    // ── Verify Expected Result ──
    await expect(page.getByRole('link', { name: ' Products' })).toBeVisible();
  });
});